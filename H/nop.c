#include "monty.h"
/**
 * nop - doesn't do anything.
 * @stack : stack.
 * @line_number: line number
 * Return: nothing.
 */
void nop(stack_t **stack, unsigned int line_number)
{
(void)stack;
(void)line_number;
}
